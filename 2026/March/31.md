#DSA: 
- https://neetcode.io/problems/minimum-stack/question : Learnt how to solve this using simply 1 stack<int> and not use stack<pair<int,int>> or similar DS using double space. 
  Used a minVal variable to store the min val and used this to encode the value stored in the stack. when popping /getting top elment, simply decode it using the same min value.

# Shell Project:
- Built a simple shell that runs on a while(1) loop untill the user pressed the "kill" command. Used fork(), execvp() and waitpid() sys calls. 
  Also learnt to be carefull while using char pointers (strings) in C, as they're tricky to handle due to memory issues.
